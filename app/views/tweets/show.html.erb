<div class="container mx-auto my-8 px-4">
  <h1 class="text-4xl">Tweets</h1>

  <div class="bg-white rounded shadow p-4">
          <p class="text-lg font-semibold">Content:</p>
      <p class="leading-normal"><%= @tweet.content %></p>
        <div class="flex items-center justify-between mt-4">
      <%= link_to 'Back', tweets_path, class: "btn btn-default" %>
      <div>
        <%= link_to 'Edit', edit_tweet_path(@tweet), class: "btn btn-default" %>
        <%= link_to 'Destroy', edit_tweet_path(@tweet), class: "btn btn-warn" %>
      </div>
    </div>
  </div>

</div>
